import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Job Tracker App",
  description: "A responsive job tracking application built with React and Next.js to manage job applications in one place. Users can add, edit, delete, search, and filter job applications by status. The app uses React state management and localStorage to persist data across sessions, with a clean and responsive UI built using Tailwind CSS.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <body>{children}</body>
    </html>
  );
}
